{
  "name": "dss-salud-ocupacional-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "src/index.js",
  "scripts": {
    "lint": "node --check src/index.js && node --check src/riskEngine.js && node --check src/alertEngine.js && node --check src/alertSummary.js && node --check src/riskIndicator.js && node --check src/absenceIndicator.js && node --check test/alertEngine.test.js && node --check test/alertSummary.test.js && node --check test/riskIndicator.test.js && node --check test/absenceIndicator.test.js && node --check test/volume.test.js",
    "test": "node --test",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0"
  }
}
